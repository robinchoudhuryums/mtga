---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
  F1 (Critical) — load_card_meta() read stale library Synergies for every OWNED card,
                  bypassing the corrected pool store K-09 points at
  F4 (Medium)   — CLAUDE.md's tier-floor spread claim was stale
  F8 (Low)      — ROADMAP.md's tier-letter spread was stale

Files modified: scripts/deck.py, CLAUDE.md, ROADMAP.md,
  decks/15-air-nomads/deck.txt, decks/40-paradox-drive/deck.txt,
  dashboard.html + scripts/role_baseline.txt (regenerated by `make postedit`)

CHANGES:

F1 | scripts/deck.py `load_card_meta` | SYNERGIES are now POOL-first; COLOURS stay
library-first. K-09 says `--merge` can only ADD to a library cell, never remove a tag
the corrected rules no longer derive, "so the pool is the corrected store" — true about
the FILE, false about the MODEL, because this loader took the library row for every
OWNED card, i.e. exactly the cards BS8-31 corrected. A BLANK pool cell never overrides
(the pool has ~340 blanks; measured at 0 such cards today, the guard is for the next
rebuild). Ends with `alias_front` as before, so check_dfc's `_ALIASED_LOADERS` entry
stays valid.

  MEASURED, before -> after, over the whole roster:
    tag sets changed        483 list-level = 225 real SET changes + 258 pure re-orderings
    stale tags REMOVED      sacrifice 131 · ramp 41 · reanimator 32 · removal 16
                            · evasion 1 · fear 1 · undying 1
    corrections GAINED      recruit 6 · Equipment 4 · storied 3 · Food 2 · cycling 1 …
    TIER FLOORS MOVED       0 of 113   <-- the K-14 test; tier_band grades on role_tally,
                                           which reads TEXT, so tags cannot reach it
    central-theme set moved 36 of 113
    cuts top-3 changed      27 of 113  (e.g. deck 8: Goldvein Pick enters the top 3)

  THE FIRST MEASUREMENT OF THIS DIFF WAS WRONG AND READ AS A CLEAN ZERO. It called
  `rank_cut_candidates(cards, meta, cd, cardmeta)`, `tier_band(cards, m, cd)` and
  `_central_themes(cards, meta)` — every signature wrong (the real ones are
  `rank_cut_candidates(d)`, `tier_band(vec)`, `_central_themes(theme_w, frac)`), so all
  113 decks fell into `except` and reported "0 moved" on all three axes. That is G-01's
  documented shape: a wrong accessor returns a clean zero that reads as a finding. The
  rerun asserts >100 decks produced a real ranking before reporting.

F4 | CLAUDE.md | "the spread is healthy (A 63 / B 43 / C 9 …)" -> "(A 62 / B 45 / C 6 …)",
re-measured live from `deck.tier_floor_spread()`. The 55% top-band figure was and is
correct; the three counts were not. This sentence is the stated EVIDENCE for leaving
`TIER_FLOOR_REQ` alone, and `figure_drift` does not cover it (6 registered figures).

F8 | ROADMAP.md | "A 39 / B 65 / C 3 / ungraded 3" -> "A 39 / B 66 / C 4 / ungraded 6",
re-measured from the roster's `#: tier:` headers.

CONSEQUENTIAL EDITS (not findings — my change made two prose figures stale, and
`check_all`'s rationale sweep caught it in the same run):
  deck 15 `#: tier:` "19 central themes" -> 18
  deck 40 `#: tier:` "20-theme spine"    -> 19
  Both re-audited with `tier <id> --audit-rationale`: rationale is current.

TEST RESULTS: passed. `python3 scripts/check_all.py` — All invariants hold ✓, back to
the single pre-existing soft warning (4 dead library searches, G-75, accepted). pytest
1729 passed / 1 skipped — the skip is test_app_editor.py's Flask importorskip on a box
without requirements-app.txt; CI installs both and does not skip. `make postedit` clean,
role baseline unchanged at 499.

REGRESSION RISKS: One was identified up front and MEASURED TO ZERO. The library's
Synergies column mixes machine-derived and hand-curated tags with no marker, so a
pool-first rule could silently delete a hand-curated tag. Checked against the tagger's
vocabulary: only two tags in the whole library are outside it — `fear` on Wraith,
Vicious Vigilante and `undying` on Shadow of the Goblin — and BOTH are Scryfall
ability-NAME artefacts, the exact K-01 shape. Wraith's ability is named "Fear Gas" and
its text is "can't be blocked"; K-01 names Shadow of the Goblin by name as having "no
Undying mechanic at all". Dropping them is a correction, not a loss, and both cards are
live (decks 54a and 55b). Return shape, memoization and `alias_front` are unchanged, so
the 20 deck.py call sites plus build_dashboard / wishlist / check_agreement /
check_colors / check_suggest / check_dfc are unaffected.

INVARIANTS AT RISK: None. No CSV was written (INV-01/01b/02/03 untouched). The two deck
edits are `#: tier:` PROSE, not card lines — INV-04 re-validated green. `load_card_meta`
still returns `alias_front(meta)`, so check_dfc's registry entry holds.

NET SCORE: 1 production fix − 0 new failure modes = 1
  (plus 2 documentation corrections and 2 consequential prose re-groundings, counted
  separately rather than folded in — F4/F8 fix stale claims, not firing bugs.)

OPERATOR ACTIONS / DEPLOY:
- None.
Deploy: Presentation — `.github/workflows/pages.yml` rebuilds and publishes the dashboard
on push to `main`. The committed dashboard.html was refreshed by `make postedit`.

FOLLOW-ON ITEMS:
- Batch 2 F1b (a cross-file `Synergies` agreement check in check_agreement) is now
  UNBLOCKED — it would have caught this the day BS8-31 shipped, and it must land after
  this fix or it goes red on arrival.
- Batch 2 F3 (extend `figure_drift` past its 6 hand-kept entries) should now include the
  tier-floor spread, since F4 is the second stale-evidence figure it did not cover.
- ROADMAP.md line 12 also claims "of which 51 are PROVISIONAL" — not re-measured, out of
  this batch's scope.
- The 258 pure re-orderings mean the `synergies` LIST ORDER now comes from the pool.
  Nothing measured moved, but any surface that shows "the first N themes" now shows the
  pool's order.
- G-30 says "Run it before a tune" of `deck.py rotation`, while its absence from
  /tune-deck is a DELIBERATE choice (recommendations should ignore rotation so the human
  decides). A future session reading G-30 will try to re-add it; the rule should record
  the exception.

DOCUMENTATION UPDATES NEEDED:
- K-09 still reads as though "the pool is the corrected store" is a fact about the FILES.
  It is now true of the MODEL as well, and the reasoning is in `load_card_meta`'s
  docstring. K-09 should say so, and name this as the fix. -> /sync-docs
---END BROAD SCAN IMPLEMENTATION SUMMARY---
