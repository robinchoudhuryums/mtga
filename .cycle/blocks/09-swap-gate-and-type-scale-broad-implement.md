---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: 
  1  Wire `unmet_gate_note` into `swap --add` — the primitive existed, was tested, and its
     ONLY caller was `cmd_redundancy`. `swap` is the surface G-06 mandates for grading a
     change and it never asked. Textbook G-40.
  2  A chosen-type payoff overlay — a card whose output scales with a CHOSEN creature type
     names no type, so K-13's trap is structural: a literal type-name search returns
     nothing and reads as a finished answer. Orcrist, Goblin-cleaver was argued down twice
     in one pass on two claims that were both false, neither of which came from a tool.
  3  A self-consuming-resource flag (G-42's unflagged mirror) — MEASURED AND DECLINED.
     Not built. The measurement is below so it is not re-derived.

Files modified: scripts/deck.py, scripts/check_patterns.py, tests/test_deck.py,
                dashboard.html (regenerated by `make postedit`)

CHANGES:

[1] | scripts/deck.py (`_do_swap`), tests/test_deck.py
    `swap` now prints `⚠ gate: <label> — 0 in this deck` between the add's oracle text and
    the deltas, when the add brings a gate the post-swap deck cannot satisfy (G-66/G-76).
    The probe is built from the POST-CUT list, not the pre-swap deck: the card being cut
    may be the only thing satisfying the add's gate, so the pre-swap list answers the wrong
    question. Pinned behaviourally — same deck, same add, opposite verdict depending only
    on whether the cut has been applied (Hobbit Hole into a list holding exactly one
    Halfling). Wrapped in try/except so a heuristic failure can never fail a swap, the same
    discipline the recommendation ledger already follows.
    Two tests added: the CLI reports it, and the post-cut probe is load-bearing.

[2] | scripts/deck.py, scripts/check_patterns.py, tests/test_deck.py
    New primitives beside the doubler / cost-scale family:
      `type_scale_payoff(text)`   — is this a chosen-type payoff? 46 pool cards, minus the
                                    two BLANKET CONVERTERS (Arcane Adaptation, Leyline of
                                    Transformation), which INVERT the term: they make every
                                    creature the chosen type, so they are worth more the
                                    more scattered your types are. Counting them would rank
                                    them backwards. A converter scoped to ITSELF (Metallic
                                    Mimic, Adaptive Automaton) is not that shape and stays.
      `type_scale_support(cards, carddata)` — (count, type): the BIGGEST creature type the
                                    deck can field, since the chooser picks on resolution.
                                    Subtypes via `creature_subtypes` — the same parser
                                    `cut_keep_score`'s `tribal` term uses, so the two cannot
                                    disagree (G-70). Changelings count toward every type
                                    (G-59). Ties broken on the name (G-54).
      `type_scale_boost(support)` — bounded, zero below the floor, measured FROM the floor.
    Calibrated from the ROSTER, per `_DOUBLER_CALIB`'s lesson: across 113 decks the largest
    creature-type count runs min 3 / p25 7 / p50 9 / p75 13 / p90 17 / max 26. Floor 7 (p25),
    key 13 (p75), cap 12 reached at 18 (~p90). 23% of decks sit below the floor and 9% pin
    the cap — the term SPANS its range, which is the check the `triggers` axis failed. A
    test re-runs that spread against the live roster rather than trusting today's numbers.
    Wired into `cmd_suggest_homes` (bounded fit bump + KEY promotion at 13, same contract as
    the fixer / doubler / cost-scale overlays: promotes only, never demotes, never overrides
    a KEY earned on theme) and into `cut_keep_score` via `_cuts_type_scale_adj` (raises only).
    The deciding COUNT is printed on every `suggest-homes` row as `✦ N <type>` with a legend
    — G-52: the failure this closes was a stated type count that came from nobody's tool, so
    the label alone is not enough.
    Four regexes registered in `check_patterns._pattern_groups()` (raw corpus) with the
    quiet-direction consequence of each going dead.

    ROSTER DIFF (measured by toggling `type_scale_payoff` off and re-running):
      suggest-homes  15 of 43 reachable family cards changed their top-5, 5 changed #1,
                     15 changed their KEY count. Every visible move is toward tribally
                     dense decks — Adaptive Automaton 57 → 74 (18 Dwarves), Chronicle of
                     Victory 57 → 28a (16 Soldiers), Patchwork Banner 57 → 76 (20 Dragons).
      cuts           0 of 113 decks changed their top-3, 0 changed #1. 3 of the 14
                     maindecked family cards moved DOWN the cut list — Banner of Kinship in
                     deck 48 (13 Robots) went from the 5th most cuttable card to the 7th,
                     which is the mis-rank the term exists to correct.
      tier floors    0 of 113. The role/quality vector is untouched by construction.

[3] | NOT BUILT — measured and declined.
    Broad form (a card that consumes a resource the deck's other cards COUNT): 44
    (card, deck) pairs fire on the live roster across 13 decks. **23 of the 44 read
    BACKWARDS**, in two families:
      · 12 FETCHLANDS (Evolving Wilds, Terramorphic Expanse, Vibrant Cityscape, Hobbit Hole,
        Elven Passage). They sacrifice themselves and REPLACE what they sacrificed — the
        land count is unchanged, and in a landfall deck the sacrifice is the payoff.
      · 11 SACRIFICE OUTLETS in decks whose plan is sacrificing (Legion Extruder, Castle
        Doom, Technodrome, Ahriman, June, Killmonger, Tom Bert and William, and Esoteric
        Duplicator, which is a sacrifice PAYOFF). That is G-41's cost-as-UPSIDE, which
        already has a flag (`⚡`) saying the opposite thing.
    Of the remaining 21, at best 3–6 are real, and every one of those is an OPTIONAL
    activated ability — a tension, not a defect. Precision ≤14%, against the ~45% at which
    G-27 declined the `#: notes:` staleness scan for exactly this reason: a standing warning
    that is usually wrong trains you to ignore the real ones.
    Narrow non-optional form (a card that unconditionally empties a zone the deck depends
    on — G-42's first named example): **0 live instances on the roster.** A gate with no
    probe it can hold is the vacuous-probe shape G-63 records.
    THE STRUCTURAL REASON, which is the transferable part: a sacrifice is a CHOICE, and the
    identical text is cost-as-upside in one deck and a conflict in another. The two readings
    are separated by the deck's PLAN, which no text model here holds. That is why G-42 says
    the genuine instances are found by COUNTING what the deck depends on — the human read
    G-61 already mandates — and not by reading the card.

TEST RESULTS: passed.
  · `python3 scripts/check_all.py` — "All invariants hold. ✓", 1 soft warning (the
    pre-existing dead-library-search sweep, unchanged from session start).
  · Full pytest suite — **1775 passed, 0 failed, 0 skipped** (was 1761: +2 fix-1 tests,
    +12 fix-2 tests).
  · `check_patterns` 344 patterns all live; `check_suggest` OK; `check_commands` OK.
  · Regression Scenario 2 (Analyze a deck — the scenario whose Subsystem overlaps deck.py):
    PASS. check / mana / consistency / tribes / stats / shape / legal / cuts / tier /
    tier --audit-rationale / redundancy / targets / text / flex / engines / rotation on
    deck 39; audit / similar / resolve / suggest --owned|--lands|--needs / screen /
    feedback / preflight / quality --json / brawl / wildcards / suggest-homes /
    pool.py --role; `deck.py --help` and `swap --help`. No traceback anywhere.
  · Scenarios 1, 3, 4 NOT APPLICABLE — no ingest, refresh or editor file touched.
    Scenarios 5–8 and 10–19 NOT APPLICABLE (browser/perceptual; no template or page
    source changed — dashboard.html is a regenerated artifact, not an edit).

REGRESSION RISKS:
  · `cmd_suggest_homes`' results tuple grew from 8 fields to 9. It is built and consumed
    inside that one function; all four index reads (r[6], r[7], r[8], the unpack) were
    updated together and `check_suggest`'s wiring anchors pass.
  · `_do_swap` gained one line of output. Return value, write path and ledger row are
    unchanged; the gate call is inside a try/except so it cannot fail a swap.
  · `cut_scoring_context` gained a key. Additive; `cut_keep_score` reads it through
    `.get(..., (0, ""))`, so a caller building its own context dict still works.
  · The new keep-bias only ever RAISES, so nothing can be ranked MORE cuttable than before.
  · Was the old behaviour ever right? For fix 2 the one case is a deck whose best type
    count is inflated by a transform DFC's BACK face — see the failure mode below.

INVARIANTS AT RISK: None. No CSV, deck file, schema or writer was touched; INV-01…06 are
untouched by construction, and `check_all` (which verifies all of them) is green.

NET SCORE: 1 production fix − 1 new failure mode = 0    (defensive: 1)
  Fix 1 — would the bug have fired in production this month? **NO, measured.** Replaying
    all 805 recommendations.csv rows since 2026-08 against the gate: 0 of 805 adds brought
    a gate their deck could not satisfy. (Approximate — it scores each deck's CURRENT list,
    not the list as it stood at swap time.) DEFENSIVE, and exactly the bucket `/reflect`
    was adopted to record. New failure mode? NO — the note is identical in text and
    semantics to the one `redundancy` already prints, and nothing about the primitive
    changed. Residual worth knowing: G-66's own token-economy caveat now reaches a second
    surface — a deck whose resource is TOKENS can read false-thin, so read the flag as a
    claim about the count, not a verdict on the card.
  Fix 2 — would it have fired this month? **YES, twice, in this session**: Orcrist was
    rejected for deck 39 on a hand-asserted type count, then re-argued. Measurably live:
    15 of 43 family cards re-order, 5 change #1, 3 of 14 maindecked copies move down the
    cut list. New failure mode? **YES, 1, documented in the docstring**: `creature_subtypes`
    walks EVERY face of a type line, so a transform DFC contributes its back-face types to
    the ✦ count; and the count reads CREATURES, while a few members of the family say
    "permanent you control of that type". Both directions are small, and reusing the shared
    parser was chosen over a second one on purpose (G-70) — a fix to `creature_subtypes`
    fixes both callers.
  Fix 3 — not built, so it scores nothing either way.

OPERATOR ACTIONS / DEPLOY:
- None.
Deploy: Presentation — `.github/workflows/pages.yml` republishes dashboard.html on push to
main. Nothing to run by hand; `make postedit` already regenerated the committed copy, and
it carries no `[analysis error` (Scenario 19's static half).

FOLLOW-ON ITEMS:
- `unmet_gate_note` still has only two callers. `suggest` and `suggest-homes` recommend
  cards into decks and neither asks — the same G-40 shape one surface over. Not done here:
  `suggest-homes` iterates every deck for one card, so it needs a cost measurement first.
- `creature_subtypes` walks all faces of a DFC type line. It has fed `cut_keep_score`'s
  `tribal` term for as long as that term has existed and now also feeds `type_scale_support`.
  Worth a front-face decision (a transform DFC's back types apply only after transforming; a
  modal DFC's arguably always) — G-63's column list does not yet name TYPE on this path.
- The NAMED-type half of fix 2 is unbuilt: a literal tribal lord ("Dwarves you control get
  +1/+1") is a much larger population and is partly served by tribal tags, so it was left
  out to keep the family measurable. It is the obvious next widening, and G-67's triage line
  applies — a pattern hole is measured, a taxonomy hole re-scores the roster.
- `type_scale_support` is recomputed per deck inside `cmd_suggest_homes` AND per context in
  `cut_scoring_context`. Cheap (O(cards)), but it is a second walk on a hot path.

DOCUMENTATION UPDATES NEEDED:
- A new gotcha anchor for the chosen-type payoff family (rule in CLAUDE.md Common Gotchas +
  long form in docs/gotchas.md — `check_docs.py` gates the link in BOTH directions, so the
  two edits must land together). It belongs next to G-83 (a COST that scales with a deck
  count) and G-33 (`_DOUBLER_CALIB`), and it should carry the calibration table and the
  BLANKET-CONVERTER exclusion, since both are the kind of thing a later reader simplifies
  away. Cross-reference from K-13, whose blind spot this is.
- G-06 / G-66 should say that `swap` now reports an unmet gate on the add, and that the
  probe is POST-CUT.
- G-42 should record fix 3's measurement (44 pairs, 23 backwards, ≤14% precision, 0 live
  instances of the narrow form) beside the sentence that already says the mirror has no
  flag — the file's own convention for a fix that was tried and declined.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
