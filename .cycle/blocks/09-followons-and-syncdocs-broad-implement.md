---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented: the four FOLLOW-ON ITEMS from block
`09-swap-gate-and-type-scale-broad-implement.md`, then `/sync-docs`.
  F-a  `unmet_gate_note` has only two callers — `suggest`/`suggest-homes` recommend cards
       into decks and neither asks (G-40 one surface over). Needed a cost measurement.
  F-b  `creature_subtypes` walks all faces of a DFC type line — front-face decision.
  F-c  The NAMED-type half of the chosen-type overlay is unbuilt.
  F-d  `type_scale_support` is computed twice per deck on a hot path.
  DOCS Four-check sync + apply.

Files modified: scripts/deck.py, scripts/check_patterns.py, scripts/check_docs.py,
                tests/test_deck.py, CLAUDE.md, docs/gotchas.md,
                dashboard.html (regenerated by `make postedit`)

CHANGES:

[F-a] DONE | scripts/deck.py, scripts/check_patterns.py, tests/test_deck.py
  COST MEASUREMENT FIRST, as the item demanded: `target_counts` costs ~2.4ms per deck —
  0.27s for the whole roster, 8% of `suggest-homes`' 3.5s — and both surfaces need it only
  on rows they PRINT, the scope `pipwarns` already uses. Cheap. Wired.
  `unmet_gate` was split out of `unmet_gate_note`: the QUESTION in one place, the RENDERING
  in its callers, because a table row has its own deck column and the single-card wording is
  redundant there. One predicate, three surfaces.

  THE WIRING EXPOSED TWO DEFECTS IN THE PRIMITIVE, AND THAT IS THE REAL FINDING. Replaying
  `suggest --unowned --limit 40` across the roster: 82 dead-gate hits over 4,520 craft picks,
  of which **12 were real**. A primitive correct enough for a single-card surface was 85%
  wrong on a 40-row table.
    · SELF-SUPPLY (40 of 82, 49%) — `target_counts` excludes the card itself, right for "does
      this deck hold targets" and wrong for "will this be dead if I ADD it". Huatli, Poet of
      Unity searches for a Dinosaur in chapter III and CREATES TWO DINOSAUR TOKENS in chapter
      I. Fixed in `unmet_gate` only (`_supplies_own_gate`), leaving `targets` untouched,
      because the two functions genuinely ask different questions.
    · REMINDER TEXT (17 of 82, 21%) — K-09's rule, which this model was breaking: `sac_a`
      matches "sacrifice an artifact", most of BARGAIN's reminder, so every bargain card
      reported a hard artifact requirement for an OPTIONAL cost three card types satisfy.
      Gates now match reminder-stripped text, EXCEPT the library-search family
      (`_TARGET_KEEP_REM`) — a fetch rider lives ONLY in its reminder, and a global strip
      would have deleted G-75's own worked example silently.
    · Then a THIRD, which the first two uncovered: G-66's stated TOKEN residual became the
      dominant false family among the survivors (17 of 25). `_makes_token` counts an
      artifact/creature token PRODUCER as fodder. See INVARIANT/REGRESSION below for its
      measured blast radius on `targets`.
  AFTER ALL THREE: 82 → 12 hits, and **12 of 12 verified genuine by hand** (The Lion-Turtle
  ×6 into Lesson-less decks, Magnetic Snuffler ×3 / Ezrim ×1 into decks with neither artifact
  cards nor artifact tokens, Colossal Rattlewurm ×2 into Desert-less decks).
  The footer wording was corrected on both surfaces to G-75's rule — a hit is a claim about
  the GATE, never the card; The Lion-Turtle with no Lessons is still a mana dork that gains
  3 life, it just cannot attack.

[F-b] DECLINED, measured | no code change
  Front-only is WORSE than the status quo, which is why the item is closed rather than done.
  Of the 35 roster DFCs whose faces carry different creature subtypes, **25 are
  creature→creature transforms** (all-faces over-counts) but **10 have a NON-creature front**
  — a Saga or enchantment whose back genuinely is that creature type, where front-only counts
  a hard ZERO. It changes 23 of 113 decks' best-type count and trades 25 over-counts for 10
  silent zeroes, the worse direction. The correct rule needs a per-face availability
  distinction the stored type line does not carry.

[F-c] DECLINED, measured | no code change
  124 pool cards name a specific creature type in a scaling clause, but `cut_keep_score`'s
  `tribal` term already serves the 92 where the card IS that type — leaving 32, a small
  increment over an existing term, and the noun extraction visibly misfires on it (it pulls
  `equipment` and `food`, which are artifact types). G-67's triage line: a TAXONOMY widening,
  not a pattern hole. The chosen-type half had no such overlap, which is why it went first.

[F-d] DECLINED, measured | no code change
  45ms across all 113 decks = **1.3%** of `suggest-homes`. A cache keyed on an unhashable
  list would be a worse trade than the 1.3% (G-71's shared-mutable-state hazard).

[DOCS] CLAUDE.md, docs/gotchas.md, scripts/check_docs.py
  Four checks run. (2) subsystem file lists and (3) operator state: NO drift — nothing moved,
  no new manual setup. (1) and (4):
    · G-66 — its stated TOKEN residual is CLOSED; deck 58's artifact-sac gates read 1 → 17,
      and its prescribed `#: notes:` workaround (a deck documenting around a model bug) is
      retired. Replaced with the rule it earned: A REPORT-ONLY RESIDUAL IS PRICED AT THE
      SURFACE THAT READS IT, so wiring a primitive to a RANKING surface re-prices every
      residual it carries.
    · G-06 — `swap` reports an unmet gate on the add, probe built POST-CUT.
    · G-40 — `unmet_gate` added as the bigger instance, with the new half: reaching a new
      caller is not free, re-measure the primitive AT that caller.
    · K-09 — the rule now binds `target_counts`, minus the library-search family.
    · G-42 — the declined self-consuming-resource measurement recorded.
    · NEW ANCHOR G-84 — the chosen-type payoff family, rule in CLAUDE.md + long form in
      docs/gotchas.md (check_docs gates the link both ways).
    · K-13 — cross-references G-84 as its automated half.
    · check_docs `figure_drift` — G-84's pool population registered, measured through
      `type_scale_payoff` itself rather than a second copy of the predicate.
  The line-cap gate (CLAUDE.md holds the rule + live residual; evidence goes to gotchas.md)
  rejected four bullets on the first pass and the evidence was moved, which is the gate
  working as designed.

TEST RESULTS: passed.
  · `check_all.py` — "All invariants hold. ✓", 1 soft warning (the pre-existing
    dead-library-search sweep, and it still reports the SAME 4 searches, which is the proof
    the reminder-scoping did not silently kill that gate).
  · Full pytest — **1783 passed, 0 failed, 0 skipped** (was 1775: +8 new tests).
  · `check_docs` OK (110 rules linked); `check_patterns` all live; `check_all` green.
  · Regression Scenario 2 (Analyze a deck) walked in full — 15 per-deck subcommands plus
    audit / similar / suggest --owned|--lands|--needs|--interaction|--ramp / screen /
    preflight / quality --json / brawl / wildcards / feedback / suggest-homes /
    tier --audit-rationale / --help. No traceback.
  · Scenarios 1, 3, 4 and the browser scenarios NOT APPLICABLE — no ingest, refresh, editor
    or template source touched.

REGRESSION RISKS:
  · `target_counts` is the one change reaching beyond the finding, and it was measured before
    being kept: gate rows across 113 decks **416 before, 416 after** (so no gate appeared or
    vanished), 32 decks' counts rose, **1 `✗ NOTHING` row became satisfied, 0 new ones**.
    `deck.py targets` and the `check_all` G-75 sweep are its other consumers; the sweep
    reports the identical 4 searches.
  · `_TARGET_KEEP_REM` is the delicate part: strip reminders for one gate family too many and
    a live gate disappears silently. Pinned by a test asserting Hobbit Hole's Halflingcycling
    rider still reports 0.
  · `unmet_gate_note`'s signature and output are unchanged, so `redundancy` and `swap` are
    untouched; `_supplies_own_gate` can only ever SUPPRESS, so its failure mode is the
    silence that existed before the gate did.
  · `cmd_suggest_homes`' results tuple grew 9 → 10 fields; all index reads updated together.
  · Where was the old behaviour right? `_TYPE_SCALE_CONVERTER_RE` — see below.

INVARIANTS AT RISK: None. No CSV, deck file, schema or writer touched; `check_all` green.

NET SCORE: 2 production fixes − 0 new failure modes = 2    (defensive: 0)
  F-a: would the bug have fired this month? **YES, measured** — 82 dead-gate hits across
    4,520 live craft recommendations in 52 of 113 decks, every one a wildcard spend the tool
    was recommending without checking. Counted as TWO production fixes, because the two
    primitive defects it exposed (self-supply, reminder text) each also corrupted `swap` and
    `redundancy`, which had been shipping them since those surfaces existed. New failure
    mode? NO — every change here only ever SUPPRESSES a warning or RAISES a count, so each
    fails toward the pre-existing silence rather than toward a new false claim.
  F-b/c/d: declined, so they score nothing either way.
  NOT COUNTED, but worth recording: `figure_drift` caught a real bug in MY OWN new text on
  its first run. It flagged "46 pool cards" against a live 43 — and the gap was not a stale
  number but an over-broad `_TYPE_SCALE_CONVERTER_RE` swallowing **Lifecraft Engine**
  ("VEHICLE creatures you control are the chosen creature type"), a scoped converter sitting
  beside a genuine lord clause. Anchored to the clause start; family 43 → 44, and the docs
  now state 44 with the 46/2 split shown.

OPERATOR ACTIONS / DEPLOY:
- None.
Deploy: Presentation — `.github/workflows/pages.yml` republishes dashboard.html on push to
main. `make postedit` regenerated the committed copy; no `[analysis error` in it.

FOLLOW-ON ITEMS:
- `unmet_gate` now has three callers and `redundancy` is the one that never got re-measured
  at ITS surface — the lesson this pass just wrote down. It is a small list, so the rate is
  probably fine, but "probably" is what this block spent the day disproving.
- G-84's DFC front-face question is closed as "all-faces is the better approximation", not as
  "correct". A per-face availability read (transform vs modal vs saga-back) would settle it,
  and G-63's column list still does not name TYPE on this path.
- `figure_drift`'s registry now holds 11 of CLAUDE.md's ~1,100 numeric claims. It earned its
  keep twice today; the registry is still hand-kept and its misses still invisible.

DOCUMENTATION UPDATES NEEDED: None — this block includes the `/sync-docs` pass.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
