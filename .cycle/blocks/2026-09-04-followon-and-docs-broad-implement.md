---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- DOCS | G-35 / G-37 brought current with the P1 breadth credit and the P2 shockland fix;
  deck 57's stale tapland note re-grounded.
- FOLLOW-ON (`match` term) | ATTEMPTED, MEASURED, REVERTED — it surfaced a defect in a
  shared primitive that is larger than the follow-on. Described below, not fixed.
NOT STARTED: P3, P4, P5 — stopped at the first follow-on per the skill's rule.

Files modified: CLAUDE.md, docs/gotchas.md, decks/57-tempest/deck.txt, dashboard.html
(regenerated by `make postedit`)

CHANGES:
DOCS | CLAUDE.md | G-35 gains the recommender-disagreed-with-the-source-count line plus
     the LIVE residual on `land_production`'s `free` set; G-37 gains the shockland
     duplicate-predicate fix and the measured 22-of-115 re-rank with
     `_LAND_BREADTH_PER_COLOR` named as the dial. Both bullets were already near the
     15-line cap, so older evidence moved out to keep them under it — `check_docs`
     enforced this and rejected two drafts before the third passed.
DOCS | docs/gotchas.md | Two new sections carrying what came out of CLAUDE.md: the
     `multi`-saturation measurement (rank 45-47 of 216, base 8.0 for both a WUR fetch and
     a UR dual) and why the fix is additive; the three-way conflation in
     `land_production`'s `free` set; the `·tapped?` duplicate predicate; and the breadth
     credit's measured cost with the 0.2-gap / +1.3-synergy explanation.
DECK | decks/57-tempest/deck.txt | The `#~` land note claimed `consistency` miscounts a
     shockland and told the reader to correct the printed count mentally. P2 closed that,
     so the note now states the real figure (2 unconditional taplands of 11 nonbasics) and
     records the caveat as closed. I had said THREE notes carried this caveat; on grep it
     was ONE — the overcount is corrected here.

TEST RESULTS: passed. Full pytest suite exit 0, no F/E markers; `check_all.py` "All
invariants hold ✓"; `check_docs.py` "Doc structure: OK (109 rules linked)";
`make postedit` clean; deck 57's rationale audit reports current.

REGRESSION RISKS: None from what shipped — this run changed documentation and one deck's
prose only. The `match` change that would have carried risk was reverted; `git diff`
confirmed the working tree matched HEAD for scripts/wishlist.py before the docs work
began, and the wishlist tests pass unchanged.

INVARIANTS AT RISK: None. No CSV, schema, deck LINE or derived data path touched
(dashboard.html is a regenerated artefact, INV-03 verified by check_all).

NET SCORE: 1 production fix − 0 new failure modes = 1
  DOCS: would it have fired this month? YES — deck 57's note actively instructed a reader
  to mentally correct a number that is now correct, which is worse than silence.
  The reverted `match` change is scored as neither: it never shipped.

OPERATOR ACTIONS / DEPLOY:
- None | BLOCKS DEPLOY: N
Deploy: commit/push. `.github/workflows/pages.yml` rebuilds and publishes the dashboard on
push to `main`; this branch does not touch that path.

FOLLOW-ON ITEMS:
- **`lib.land_production`'s `free` set conflates three different behaviours**, and this is
  now the blocking item for any breadth-aware land scoring. Measured: 27 Standard lands
  report 3+ free colours; of those, (a) some genuinely produce any colour every tap
  (Starting Town), (b) SEVEN choose ONE colour on entry and supply only that colour for
  the rest of the game (Uncharted Haven, Night Market, Valgavoth's Lair, Crossroads
  Village, Mirage Mesa, Edgewall Inn, Tarnation Vista), (c) Branch of Vitu-Ghazi taps for
  `{C}` and its any-colour line is reachable only by turning it face up via Disguise — it
  scored 10.0, the maximum — and (d) Scene of the Crime's any-colour ability costs an
  extra "tap an untapped creature" and is filed `free`. The `match` fix is correct in
  isolation (an any-colour land scored 8.4 against a fetch's 8.8) but moved the #1 land
  pick in **89 of 115 decks** on top of this, so it was reverted. FIX THE PRIMITIVE FIRST;
  the same `free` set feeds `deck_source_profile`, so source counts for decks running
  those nine lands are also affected.
- After that primitive is fixed, re-measure `_LAND_BREADTH_PER_COLOR` — the P1 credit is
  gated on `len(used) > 2` and therefore also over-credits those nine lands today. Part of
  P1's measured 22-deck re-rank is attributable to this, not to fetches.
- P3 (repeatable-vs-one-shot report-only split), P4 (existential prose claims about the
  pool), P5 (`#~` swap-line figures outside the staleness sweep) remain open — see
  `.cycle/blocks/2026-09-04-deck-57-tune-findings.md`.

DOCUMENTATION UPDATES NEEDED: None outstanding — this run was the doc sync. G-35 and G-37
are current, and the `land_production` residual is recorded in both CLAUDE.md (short form)
and docs/gotchas.md (with the measurement) so the next session finds it.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
