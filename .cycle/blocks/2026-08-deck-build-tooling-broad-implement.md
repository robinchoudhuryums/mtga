---BROAD SCAN IMPLEMENTATION SUMMARY---
Findings implemented:
- F-01 — a deck line's `(SET) COLLECTOR#` was validated by NOTHING; `(ZZZ) 172` passed `legal`, `check` (reported OWNED), `preflight` READY and `check_all`.
- F-02 — an intentionally-uncastable reanimation target graded as a build ERROR: one 5-colour bomb moved deck 52a from preflight READY/floor A to BLOCKED/floor C.
- F-16 (latent, subsumed by F-02) — `tier_band` SET the floor to C on an uncastable stray instead of CAPPING it, so a stray RAISED a D-floor deck.
- F-04 — nothing answered "does this deck contain TARGETS for its own gated effects"; new `deck.py targets`.
- F-03 — `tier --audit-rationale` reported "rationale is current" while `#: archetype:` cited a removed card and `#: tier:` quoted a stale avg MV.

Files modified:
- scripts/deck.py (all five)
- scripts/check_all.py (F-01 — INV-04 extension + soft warning)
- scripts/check_tier.py (F-16 — the anchor pinned the OLD behaviour and had to move with it)
- scripts/check_patterns.py (registry entries for 4 new module-level patterns)
- .claude/commands/draft-deck.md, .claude/commands/tune-deck.md (F-04 — a command no workflow invokes is invisible, G-53)
- tests/test_deck.py (+36 anchors across 6 new classes)

CHANGES:
F-01 | scripts/deck.py | New `known_printings()` (memoized; front faces aliased in a SECOND pass so a `Front // Back` row cannot shadow a real card named `Front`, per G-63) and `printing_problems(cards)` returning (bad_set, unverified). A set code appearing in NO card anywhere is HARD; a real set with an unheld collector number is SOFT, because the pool keys ONE printing per card by construction so a legitimate alternate art lands there too. Basic lands are EXEMPT — measured first: Arena prints several arts per set (Swamp MSH 291 AND 292 are both real) while the pool carries one, and a hard rule without the exemption failed 61 of 78 deck files on basics alone.
F-01 | scripts/deck.py | `cmd_legal` prints both lists per deck, so the person editing the file sees the line.
F-01 | scripts/check_all.py | `check_decks()` returns warnings as well as errors; bad set codes join the hard INV-04 failures, unverified printings are summarised into one soft line. Roster today: 0 hard, 27 soft across 15 decks — all pre-existing and silent until now (e.g. `Explosive Derailment (DFT) 130` when the card is `(OTJ) 122`, `Vampire Nighthawk (FDN) 186` vs `(FDN) 757` in four decks).
F-02 | scripts/deck.py | New `#: uncastable-ok: A; B` header + `_uncastable_ok(meta)`, semicolon-separated like `#: protect:` because card names contain commas. `_castability` gains an `exempt` parameter and returns a FOURTH list, `intended` — exempt cards leave the failure list but are still printed by `mana` (`◆ Intentionally uncastable`) and counted in `preflight` as `(+N intended, exempt)`. G-52: a verdict surface must print its evidence, so nothing disappears. Deliberately OPT-IN and per-card; the default stays a hard FAIL because most uncastable cards really are mistakes.
F-02 | scripts/deck.py | `META_RE` widened to allow a HYPHEN in a `#:` key. It did not, and the deck files had been using one anyway — **24 `#: based-on:` lines across the roster were silently dropped**: the line matched no meta key, fell through to the card-line branch, matched no card, and vanished with no warning. Nothing read `based-on`, so nothing noticed. Verified nothing iterates meta keys (only named lookups), so this adds keys without changing any output.
F-16 | scripts/deck.py | `tier_band` computes the band first, then applies the stray as a CAP (`if uncastable and rank(band) > rank("C"): band = "C"`). The old form returned "C" outright, so a deck whose measurable floor was D got RAISED by holding a dead card. "Caps" is what the docstring and CLAUDE.md's rubric always said; only the code disagreed. Roster sweep: **0 decks change floor** — it was latent exactly as F-16 filed it.
F-16 | scripts/check_tier.py | `_pure_floor`, the no-regression anchor, encoded the old SET behaviour and failed 4 cases on the fix. Updated in lockstep with the reason recorded — this is the "test doubles encoding the OLD behavior" case, found by running the gate rather than by the suite.
F-04 | scripts/deck.py | New `target_counts()` + `deck.py targets <id>`: for every card whose text names a GATE (an MV cap on what it can reanimate, a sacrifice cost, a count threshold), how many cards in THIS deck satisfy it. Counts exclude the card itself and exclude lands. Flags `✗ NOTHING` (dead card), `⚠ short of N`, `⚠ thin` (≤3). The recursion row reports the count that actually decided something on deck 52 — not "how many creatures" (nearly all of them) but how many are MV5+, i.e. big enough that cheating them in gains mana. This is the automated half of G-61's "state the count, then decide", which existed only as prose with four overturned dismissals behind it.
F-04 | scripts/deck.py | A generic "cards to discard" gate was written and then REMOVED: it reported 35 for every discard outlet in a 60-card deck — "you have a hand" — which is the same saturation failure this file already documents for `suggest`'s Decks column (99%) and `cuts`' protect boost (87%). A gate earns a row only when the resource can be SHORT. Pinned by a test so it cannot come back.
F-04 | .claude/commands/*.md | Wired into `/draft-deck` Stage 4 and `/tune-deck` step 2c-bis. `check_commands` failed until this was done — working as designed (G-53).
F-03 | scripts/deck.py | `_HISTORY_CUES`: `remov\w*` → `remov(?:ed|ing)`. The broad form matched the cited card's OWN ORACLE TEXT — "Summon: Bahamut is a {9} that REMOVES two nonland permanents" — so the citation suppressed itself and the audit reported clean while the header argued from a card two swaps had cut. The identical word is already documented four lines below as "the worst of them" on the FIGURE path, where it was narrowed because `removal` is the commonest noun in a rationale about interaction; only the figure path had been fixed.
F-03 | scripts/deck.py | `_RATIONALE_FIGURES`: the avg-MV patterns read only the abbreviation `avg`. Deck 52a wrote "Average nonland MV 4.17" against a live 4.22 and no pattern could ever see it, because "avg" is not a prefix of "Average". Added `average`, and split the curve alternatives so each spelling has its own pattern.
F-03 | scripts/deck.py | Two NEW false-positive suppressors, added because the `remov` narrowing surfaced 6 citations of which 2 were noise — shipping the noise would have been the wrong trade in a file whose own doctrine is that a noisy check gets ignored. (a) SHORTHAND: a citation that is a strict word-prefix of a card the deck DOES run is an abbreviation of it (deck 33 writes "Heartfire sac-removal" for Heartfire Immolator, and `Heartfire` is itself a real card; masking cannot help because the full name is not in the text). (b) SIMILE: `_SIMILE_BEFORE` matches `is`/`are` immediately before the name — "It'll Quench Ya! is Spell Pierce that hits creatures too" explains an in-deck card BY NAMING one the deck does not run. Positional rather than a window cue on purpose: `is` in the ±140 window would suppress nearly everything, but this scan only ever sees cards the deck does NOT run, so a real "the win condition is Krang" names an in-deck card and never reaches it.
F-03 | scripts/deck.py | New `wrong_exclusion_claims(d)` + a line in the audit output. The MIRROR case G-27 deliberately leaves out: `#: notes:` is exempt from the staleness scan because naming an ABSENT card there is correct, but "Deliberately NOT included: Bringer of the Last Gift" AFTER Bringer was added is a false claim about the current list, in the opposite direction from the exemption. **Took three tries and the measurements are why it is written this way**: a ±400-char window gave 10 roster hits and every one sampled was noise; splitting the post-cue prose on `;` gave 37; reading only the CLAUSE the cue introduces (parentheticals stripped, cut at the first boundary) gives 0 roster hits and still catches the sentence that motivated it. The boundary cut is load-bearing — "Craterhoof is deliberately NOT here — Summon: Titan is this deck's mass pump" names the REPLACEMENT after the dash.

TEST RESULTS: PASSED. `python3 scripts/check_all.py` — all invariants hold, exit 0, 2 soft warnings (both NEW and both intended: 27 unverified printings, 4 stale rationale citations). `pytest` — **826 passed** (was 804; +22 net, +36 new anchors, 0 pre-existing failures). All ten `check_*` gates green standalone. Two failures occurred mid-session, both caused by this session's changes and both fixed here: `check_patterns` rejecting 4 unregistered module-level patterns (working as designed), and `check_tier` failing 4 cases because its reference implementation pinned the pre-F-16 behaviour.

Regression Scenarios walked (Subsystems overlapping modified files):
- Scenario 2 (Analyze a deck) — **PASS**. 21 invocations across `check|mana|consistency|tribes|stats|shape|legal|cuts|tier|redundancy|text|targets|engines|preflight`, roster-wide `audit`/`rotation`/`similar`/`resolve`/`feedback`, `tier --audit-rationale`, `suggest --needs`, plus `--help` at top level and on a subcommand. No traceback. (`check 52a` exits 1 — that is the WIP missing-card code, not a crash.)
- Scenario 4 (Edit via the app) — **PASS**, headless: `/` and `/decks` both 200. INV-04 changed, and `app.py` gates deck saves on it, so this was checked rather than assumed; the new hard rule fires only on a set code that exists nowhere, which the app cannot produce from an existing line.
- Scenarios 1, 3 — **NOT APPLICABLE**. No ingest or derived-data builder was modified.
- Scenarios 5–8 — **NOT APPLICABLE**; perceptual/browser checks, and nothing here touches status colours, the responsive layer or the editor's failure toasts.

REGRESSION RISKS:
- `_castability` changed arity (3-tuple → 4-tuple). All five call sites updated; `_candidate_castability` is a DIFFERENT function and was not touched. Grep-verified, and the suite exercises all five paths.
- `META_RE` widening makes `#: based-on:` (24 lines) parse for the first time. Verified nothing iterates meta keys — every read is a named lookup — so no surface gains a field. A stray `#: some-word:` comment would now become a key; harmless, and none exist.
- `_HISTORY_CUES` narrowing is a SUPPRESSION change, so it can only ADD flags. Measured roster-wide: 6 new, 0 lost; 2 of the 6 were false positives and were separately fixed, leaving 4 true (deck 49 cites Bonehoard Dracosaur and Nova Hellkite, both moved out of the main list THIS session). Residual: prose that legitimately says "the swap removes X" about a cut card is now flagged rather than suppressed — a false positive, which is the direction this file's doctrine prefers.
- `tier_band`'s cap: roster sweep shows 0 decks change floor. The only decks it can affect have an uncastable stray AND a D-level vector.
- `deck.py targets` is new and report-only — no existing surface reads it, and nothing feeds `tier_band`.
- The soft `check_all` warning count went 0 → 2. Both are real findings, not noise, but anyone reading "2 soft" as a regression should read the lines.

INVARIANTS AT RISK: None — re-verified directly. INV-01 `validate` rc 0. INV-02 zero library names missing a mana row. INV-03 derived files keep their own headers. INV-04 is now STRICTER (a nonexistent set code is a hard failure) and passes at 0 across all 78 deck files — measured before the rule was made hard, which is why it could be. INV-05/06 untouched.

NET SCORE: 5 production fixes − 0 new failure modes = **5**
(Per-fix: F-01 YES, it fired this month — deck 52 was written with `(FDN) 610` against a real 172, and 27 unverified printings sit on the roster now. F-02 YES — the user has stated they intend to add 1–2 gold bombs to 52a, which would have blocked `preflight` and dropped the floor three bands. F-04 YES — the 24-vs-8 count is what redirected the entire deck 52 build, and it came from a hand-written script. F-03 YES — it reported clean on 52a's stale header this session, and the fix surfaced 4 real stale citations, two of them created by this session's own edits to deck 49. F-16 NO — latent, 0 roster decks in range, exactly as filed. New failure modes: none; the one this work nearly introduced — a noisy exclusion check at 10 then 37 roster false positives — was closed by rewriting the rule around clause shape instead of distance, and pinned by a test.)

OPERATOR ACTIONS / DEPLOY:
- None. | BLOCKS DEPLOY: N
Deploy: Data + local tooling ship by commit/push (no build step). The Presentation subsystem was not modified, so no dashboard rebuild is required by this diff.

FOLLOW-ON ITEMS:
- **27 unverified printings on 15 decks, now visible and unfixed** — deliberately out of scope (this session added the check, not the corrections). Several look like genuine errors worth fixing by hand: `Explosive Derailment (DFT) 130` → `(OTJ) 122`, `Mechan Navigator (DFT) 48` → `(EOE) 64` in two decks, `Burst Lightning (SOA) 41` / `(DMU) 132` → `(FDN) 192` in three, `Vampire Nighthawk (FDN) 186` → `(FDN) 757` in four. Others may be real alternate printings the pool does not carry. Run `deck.py legal <id>` per deck.
- **4 stale rationale citations now surfaced**: deck 40 (Doppelgang), deck 49 (Bonehoard Dracosaur, Nova Hellkite — both created by this session's flex-block move), deck 51a (Lost Days). Report-only; the prose is a human argument.
- F-05 (`screen` KEY saturation, measured 45–51%), F-06, F-07, F-08, F-09, F-10 from the same scan — unimplemented, per scope.
- `#: based-on:` now parses and nothing reads it. Either use it (a variant could resolve its parent through it rather than by filename convention) or drop it from the files.

DOCUMENTATION UPDATES NEEDED:
- CLAUDE.md **INV-04**: the wording is "every deck file under decks/ parses with no malformed card lines" — it now also asserts every line's SET CODE exists. Update the invariant text and the Verify column.
- CLAUDE.md **Key Design Decisions** or a new gotcha: the `#: uncastable-ok:` header — what it is, that it is opt-in and per-card, and that the default hard FAIL is correct for everything else.
- CLAUDE.md **Analysis subsystem list [C-05]**: add `deck.py targets`; **Regression Scenario 2 [C-09]**: add `targets <id>` to the command walk.
- CLAUDE.md **G-26**: the KNOWN RESIDUAL paragraph should record that `remov\w*` was the card-path suppressor too, and that two new residual shapes (shorthand, simile) are now handled — plus the remaining one (prose saying "the swap removes X").
- CLAUDE.md **G-27**: `#: notes:` is still out of the staleness scan, but an EXCLUSION claim in it is now checked by `wrong_exclusion_claims`.
- `docs/gotchas.md`: long-form entries for the above under their existing anchors.
- `docs/cycle-config.md` [C-01]: `check_all` now carries a printing check; [C-07] tests/ is still 17 files (no new file), but the count of anchors changed.
---END BROAD SCAN IMPLEMENTATION SUMMARY---
