[pytest]
# The unit-test layer lives in tests/ (see tests/conftest.py for the scripts/ path
# bootstrap). Complementary to scripts/check_all.py, which stays the pure-stdlib
# integrity + model-sanity gate.
testpaths = tests
addopts = -q
